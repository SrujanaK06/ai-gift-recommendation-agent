# AI Gift Recommendation Agent

An AI-powered gift recommendation application built with Streamlit, Google Gemini, and LangGraph.

## Environment variable

Add this in Render:

GEMINI_API_KEY = your Gemini API key

Never put the actual API key in GitHub or directly inside app.py.

## Run

pip install -r requirements.txt

streamlit run app.py
